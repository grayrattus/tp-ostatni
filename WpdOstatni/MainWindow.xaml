<Window x:Class="WpdOstatni.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpdOstatni"
        xmlns:vm="clr-namespace:WpdOstatni.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <StackPanel Orientation="Vertical">
        <ListBox>
            <ListBoxItem>List box #1</ListBoxItem>
        </ListBox>
        <ListBox ItemsSource="{Binding Users}" SelectedItem="{Binding CurrentUser}" Grid.Column="0" Grid.Row="0" Height="266">
            <ListBox.ItemTemplate>
                <DataTemplate  >
                    <StackPanel Orientation="Vertical">
                        <Label Content="{Binding Name}"/>
                        <Label Content="{Binding Age}"/>
                        <Button>Edit</Button>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <StackPanel Orientation="Vertical">
            <Label Content="Name"/>
            <TextBox Text="{Binding CurrentUser.Name}"/>
            <Label Content="Age"/>
            <TextBox Text="{Binding CurrentUser.Age}"/>
            <Button Content="Edit user"></Button>
        </StackPanel>
        <TextBox Text="{Binding Curre}" TextWrapping="Wrap"/>
        <TextBox Text="{Binding ActionText}" TextWrapping="Wrap"/>
        <Button Content="Button" HorizontalAlignment="Left" VerticalAlignment="Top" Width="75" Command="{Binding DisplayTextCommand}"/>
    </StackPanel>
</Window>
